# BACKEND .env Configuration Guide

## Server Configuration

PORT=5000
NODE_ENV=development # Use 'production' for production deployments

## Database Configuration

# Local MongoDB

MONGO_URI=mongodb://localhost:27017/ai-ticket-manager

# OR MongoDB Atlas (Cloud)

# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/ai-ticket-manager

## Security - JWT Configuration

# Generate a strong secret key: use `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`

# IMPORTANT: Change this in production!

JWT_SECRET=6b99622b63017cd98905ef8429e77b4555d24c4eef2c185b2b542d59d7eca9890ea8c6353d68388c86320c3409c947e640da803d231d430968ffcfc61998e31f
JWT_EXPIRE=30d

## OpenAI Configuration (Optional - for AI Triage features)

# Get your API key from: https://platform.openai.com/api-keys

OPENAI_API_KEY=sk-your_openai_api_key_here

## WebSocket Configuration

WS_ORIGIN=http://localhost:5173

## Logging

LOG_LEVEL=debug # Use 'info', 'warn', 'error' in production

## IMPORTANT SECURITY NOTES:

# 1. NEVER commit .env file to git

# 2. Use strong, random JWT_SECRET (minimum 32 characters)

# 3. Keep API keys confidential

# 4. Use HTTPS in production

# 5. Add .env to .gitignore
